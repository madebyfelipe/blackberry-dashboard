# Roadmap — black berry

Documento vivo. Marca o que já existe, o que falta **desenhar** (você) e o que falta **construir** (Claude), até o fim do projeto. Última atualização: **22 set 2026** (Inbox, issue #30, a camada de tempo real da issue #43 — Ably e LiveKit — e o app de desktop em Electron).

Legenda: ✅ pronto · 🟡 parcial/placeholder · ⬜ não começado · 🎨 precisa de tela sua antes de eu construir

---

## Fundação
- ✅ Projeto Next.js 16 + React 19 + Tailwind v4 + TS do zero
- ✅ Tokens do design **black berry** (`globals.css @theme`) + primitivos (`Button`, `Input`, `StatusPill`, `Avatar`, `Breadcrumb`, `Toast`)
- ✅ **Design system v2 (gradiente)**: tokens anotados com os nomes `bb-*` do export, `--color-dim` (#616161), `--radius-panel`/`--radius-menu`, sidebar com `bg-sidebar-gradient` e corpo da lista transparente
- ✅ **Polimento do design system** (issue #14): hex e raios crus saíram do JSX para o `@theme` (`--color-danger`, `--color-badge`/`--color-badge-strong`, `--radius-thumb`, `--spacing-control`, cinzas de status), o "⋯" ganhou o alvo de toque de 40px sem mexer na altura das linhas, os menus de ação voltaram à forma do export (10px/6px) com `aria-haspopup`/`aria-expanded` e navegação por setas, e o texto de apoio em conteúdo real subiu de `faint` (2,5:1) para `muted` (5,3:1)
- ✅ **Troca de tela sem clique parado**: `app/(app)/loading.tsx` é o esqueleto de reserva de toda tela sem o seu — tela dinâmica sem borda de carregamento não é pré-carregada, e o clique na lateral ficava sem resposta até o servidor montar a tela inteira. A sessão (`currentUser` e o acesso) é lida uma vez por requisição (`React.cache`), não uma por quem pergunta
- ✅ **Contorno que fecha em todo canto arredondado** (issue #24): o par `outline` + `-outline-offset-[0.5px]` quebrava o traço num trecho da curva (meio pixel de tela). Pílulas, chips e botões passaram a `inset-ring-1` (box-shadow, mesmo recorte da caixa, sem mexer no layout), o ponto de status do quadro virou `inset` e o item ativo da lateral voltou ao export — o marcador de 3px em `left-0` era comido pela pílula de raio 24px e aparecia como lasca
- ✅ **Design system v3** (telas redesenhadas): painel de conteúdo de 28px sobre o preto, abas em pílula, barra de ferramentas (Filtros · busca · Personalizar), lista de 44px por linha com caixa de seleção e barra flutuante de ações, card de 14px comum ao Quadro e à Grade, selo de status e as duas marcas redondas. Mora em `src/components/ui/{Screen,Tabs,Toolbar,DataTable,SelectionBar,EntityCard,Badge,Mark,Popover}.tsx`; os tokens novos (incluindo a escala de cor da saúde do cliente) estão no `@theme` de `globals.css`. Aplicado em Tarefas (Lista e Quadro), Descrição da tarefa, Clientes (Lista e Grade) e no shell
- ✅ Shell autenticado (sidebar completa) + rotas placeholder
- ✅ **Lateral no v3**: fundo preto, "Ações rápidas" com o atalho `/`, caixas de entrada (Notificações, Inbox — "Conversas" saiu em 2026-09-23, pedido do Felipe: a conversa do time é o Inbox, e `/conversas` redireciona para lá) e as seções Trabalho (Tarefas, Social media, Clientes, Fluxos e Processos — no lugar de Relatórios, pedido do Felipe) e Equipe (Membros, Configurações). Os destinos ainda sem tela entraram como placeholder — nunca como tela inventada
- ✅ Camada de dados com `repository`/`store` (JSON + fallback memória) e API REST
- ✅ Sistema de toasts + animações (fade/drawer/pop) + `prefers-reduced-motion`
- ✅ **Camada de movimento** (`globals.css`): curva única de entrada, cascata de listas por `--d`, troca de tela (`template.tsx`), esqueletos (`loading.tsx`) e feedback de toque (`.tap`) — só `transform`/`opacity`
- ✅ **Shell que cabe no celular**: a lateral fixa de 256px virava 86px de conteúdo em 390px — agora é barra de topo + gaveta abaixo de `md` (provisório até existir desenho de mobile)
- ✅ **Store de arquivo compartilhado** (`lib/store/json-file.ts`) com revalidação por mtime — `next start` roda vários workers, e sem isso a tela renderizada por um worker não via o que o outro acabou de gravar

## Fase 1 — Aprovação de conteúdo (prioridade absoluta)
- ✅ **Fluxo cliente › lote › peças** (issues #26 e #27): `/social` virou a escolha do cliente (export "Clínica Aurora - Clientes"), `/social/[cliente]` traz os lotes daquele cliente (export "Lotes de Aprovação") e a tela de peças desceu para `/social/[cliente]/[lote]`. Endereço antigo (`/social/<id>` e `/social/<id>/editor`) redireciona para o novo
- ✅ **Criar lote** (issue #27, export "ADD7"): modal com título, período e descrição — o cliente vem da tela anterior, o lote nasce rascunho com link próprio e cai direto no editor. Era a dívida que deixava uma agência recém-cadastrada sem saída no `/social`
- ✅ Lote (visão agência) **no desenho minimalista** (issue #25, export "Clínica Aurora - Lote"): topo só com a trilha e o link público (URL + copiar), chips `Todas 8 · Ajuste 2 · Aprovado 3 · Pendente 3`, grade de peças de 130px com selo de status na arte e painel de detalhe na borda direita (meta, histórico, "Aprovar peça" / "Marcar como refeita" / "Abrir no editor"). As ações do link (WhatsApp, e-mail, copiar mensagem, gerar/desativar) passaram para o menu do editor, e filtro por formato e busca de peça saíram da tela
- ✅ **Aprovação pela agência** (`POST /api/batches/[id]/pieces/[pieceId]/approve`) — registra "Aprovada pela agência" no histórico, separada da decisão do cliente pelo link público
- ✅ Lista de lotes (`/social`) com progresso
- ✅ **Link público por swipe** (`/a/[token]`): arrastar p/ aprovar, reprovar com motivo, progresso, tela final
- ✅ **Swipe sem card voltando ao centro** (issue #17): cada peça é um nó novo (`key`), então a seguinte entra já no lugar em vez de deslizar da borda; o retorno ao centro quando o arrasto não passa do limite continua igual
- ✅ Peças mostram a **arte real** quando existe (`PieceThumb` serve os dois estados: com e sem mídia)
- ✅ **Upload real de mídia** (PNG/JPG/WebP/GIF/MP4/MOV, até 50 MB): "Subir artes" cria uma peça por arquivo, com nome, tamanho lido do cabeçalho e formato deduzido; trocar/remover pelo preview; `/api/media/<id>` serve a arte para o link público
- ⬜ Derivadas leves da mídia (WebP/poster de vídeo) — hoje o arquivo é servido como veio
- ⬜ Comentário por **áudio** + transcrição (Whisper) — spec
- ✅ Registro de decisão com **IP + versão exata** apresentada (`snapshot` no `DecisionEvent`, captura IP via `x-forwarded-for`)
- ✅ Token expirável/revogável + geração de link por lote (menu de ações no `/social/[id]`, expira em 30 dias ao regenerar, tela pública mostra estado "link inativo")
- 🟡 **Envio do link**: WhatsApp, e-mail e "copiar mensagem" saem do menu do lote com o texto pronto e a URL do ambiente atual — falta o disparo automático pelo servidor e os lembretes
- ✅ **Editor de lote reconstruído** (issue #2, export "Clínica Aurora - Montagem de lote Criativos"): substituiu o editor anterior (peças/legenda/hashtags/preview lado a lado) por um composer de um criativo por vez — nome, formato, carrossel de artes e legenda (atalhos de #/@) — com a lista "Criativos do lote" ao lado. Mora em `/social/[cliente]/[lote]/editor`, no fim da jornada Social media › cliente › "+" (`NewBatchModal`: título/período/descrição) › editor. `Piece.media` virou `MediaAsset[]`: uma peça pode ter várias artes (carrossel de verdade), reaproveitado por toda a aprovação (badge "1/N" no `PieceThumb`) — falta só o visualizador de carrossel no link público do cliente (issue #8)
- ✅ **Abertura do link público** (`/a/[token]`): hero com carrossel das peças, resumo do lote e CTA para o swipe
- 🎨 Tela pública: estados de **carrossel/vídeo** e visual de "mockup de feed Instagram" — falta desenho (o preview do editor já é uma primeira versão)

## Fase 2 — Operação / Tarefas
- ✅ Lista + Quadro (Kanban) sobre os mesmos dados, CRUD, busca, tabs de status, drag-and-drop, updates otimistas
- ✅ **Lista e Quadro no design system v3** (exports "Tarefas · Painel (Lista)" e "(Quadro)"): a lista virou tabela de 44px por linha com marca, cliente na segunda linha, selo de status, responsável com avatar e prazo; o quadro ganhou o card de 14px com selo, régua e rodapé. O contador da coluna, as colunas de largura igual e o alternador Lista/Quadro do v2 continuam
- ✅ Busca das Tarefas: no v3 ela mora na barra de ferramentas do corpo, ao lado de "Filtros" (o botão redondo em overlay era a forma do v2)
- ✅ **Menu de filtros** (`FilterMenu`, atalho `F`) e **menu "Personalizar"** (`DisplayMenu`), agora nos dois gatilhos da barra de ferramentas — o seletor Lista/Quadro vive dentro do "Personalizar", junto com agrupamento, sub-agrupamento, ordenação, arquivadas, tarefas por grupo, grupos vazios e propriedades visíveis (`src/lib/tasks/view.ts`)
- ✅ **Quadro: "Adicionar tarefa" só no hover da coluna** (ou no foco pelo teclado), como no comportamento original
- ✅ **Campos ricos da tarefa**: descrição, prioridade, etiquetas, criador (vem da sessão) e prazo interno — com colunas, agrupamento e ordenação por prioridade/prazo
- ✅ **Filtros de Prioridade, Etiquetas e Criador filtram de verdade**; o menu de Datas ganhou a seção de prazo (atrasadas, vencem hoje, próximos 7 dias, sem prazo)
- ✅ **Descrição da tarefa** (`/tarefas/[id]`, export "Tarefas · Descrição da tarefa"): a tela que o clique numa tarefa e a criação de uma tarefa nova abrem. Editor e visualizador ao mesmo tempo — título, cliente, descrição e todas as propriedades se alteram no lugar, com salvamento otimista e sem botão "Salvar". A coluna direita (PROPRIEDADES, 340px) tem rolagem própria e fica parada enquanto o conteúdo rola; no celular as duas viram uma coluna
- ✅ **Conversa da tarefa**: `Task.comments` + `POST /api/tasks/<id>/comments`, autor vindo da sessão. É a seção ATIVIDADE do export, junto com o registro de criação
- ✅ **Seleção em massa na Lista**: caixa por linha, "marcar todas" no cabeçalho e a barra flutuante do export (contagem, Exportar, Arquivar, Excluir, Mais). Excluir funciona com desfazer; Exportar e Arquivar avisam que ainda não existem, em vez de fingir
- ✅ **Escrita do briefing** (exports "menu_comando_tarefas", "menu_formatacao", "formatacao_hover"): hint "/ para formatação" na linha vazia, menu de comandos no `/`, menções no `@`, menu de formatação na seleção com a lista de níveis no hover, atalhos de teclado, e a descrição mostrada formatada fora da edição. O texto segue sendo `string` com marcas markdown (ver README, "O texto do briefing é markdown")
- ✅ **Colunas redimensionáveis em todas as listas** (`lib/ui/columns.ts` + `ui/DataTable`): arrastar a divisória, duplo clique para voltar à medida do export, setas pelo teclado, largura guardada no navegador
- ✅ **Edição em sequência sem voltar atrás**: na descrição da tarefa, trocar dois campos seguidos não desfaz mais o segundo na tela quando a resposta do primeiro chega depois; e o refresh da rota sai um por leva de edições, não um por campo
- 🟡 Filtros que dependem de recursos inexistentes (Agente, Sessão do agente, Relações…) seguem no desenho, avisando em vez de filtrar
- ⬜ Campos da peça que faltam: tipo, objetivo, pilar, resp. arte/texto, data de publicação, arquivo, versão
- ⬜ **Calendário editorial** (visão de mês/semana das publicações): o export da issue #2 virou o novo Editor de lote (ver Fase 1) — as decisões da issue sobre mês/semana, arrastar para reagendar etc. seguem sem resposta
- ⬜ **Carga do time**
- ⬜ Briefings por tipo de peça (chegam preenchidos ao designer)
- ⬜ Biblioteca de marca · Banco de ideias · Pauta recorrente a partir do contrato
- 💡 **Agenda da agência** (backlog de ideias, pedido do Felipe em 2026-09-25): uma tela com os eventos de todos os clientes (reunião, gravação, entrega). Hoje os eventos moram na ficha de cada cliente e o Painel da agência mostra os próximos — o "Abrir agenda" do desenho fica escondido até a tela existir 🎨
- 🎨 Telas de Calendário (mês/semana), Carga do time e Briefing — falta desenho
- ✅ **Fluxos e Processos** (`/configuracoes/fluxos`, export "Fluxos e Processos"): esteira de etapas com responsável (pessoa, squad do cliente ou o cliente), prazo, próxima etapa, aprovadores e automações; menu de contexto, cartão de hover, arrastar para reordenar
- ✅ **Clientes do fluxo** (2026-09-23): "Atribuir cliente" no fluxo aberto (o mesmo campo "Fluxo" da ficha). Tarefa criada **na mão** para um cliente com fluxo atribuído já nasce na etapa de início, com quem a toca (ou quem a criou escolheu) e o prazo dela — antes só o criativo do lote entrava. `enterClientFlow` em `lib/flows/automation.ts`
- ✅ **Cartão de hover da etapa** — dava para ver, não para usar: sumia no caminho do mouse até ele. Agora sair da etapa só agenda o fechamento, e entrar no cartão o mantém aberto
- ✅ **Novo fluxo** (2026-09-25, export "Novo Fluxo · 1. Modelo / 2. Detalhes / 3. Revisão", `/configuracoes/fluxos/novo`): três passos — modelo (Começar do zero, Social Media, Blog & Conteúdo, Paid Media), detalhes (nome, descrição, categoria, ícone e cor, "Todos os clientes" ou "Clientes específicos") e revisão ("Ativar ao criar", "Salvar rascunho", "Criar fluxo"). "Editar" no cabeçalho do fluxo abre o passo Detalhes com o fluxo preenchido (`/configuracoes/fluxos/<id>/editar`). Novo status **Rascunho** (aparece em "Inativos"). "Todos os clientes" = o fluxo padrão de quem não tem fluxo próprio; "Clientes específicos" nunca pega cliente de fora (`pickFlowForClient`). Régua em `lib/flows/{templates,wizard}.ts`, testada
- 🟡 **Novo fluxo — decisões minhas a confirmar com o Felipe**: (1) a lista de categorias (Marketing / Conteúdo, Mídia paga, Atendimento, Operação interna, Outro) e a cor do ponto de cada uma — o export só mostra a primeira; (2) a grade de ícones do "Trocar" (12 ícones lucide); (3) as etapas, prazos e statuses dos modelos Blog & Conteúdo e Paid Media — o export só dá os nomes; (4) a edição usa só o passo Detalhes, com "Salvar" no lugar de "Continuar" e sem o seletor de passos; (5) "Editar etapas" na Revisão salva como rascunho e abre o editor, porque as etapas só se editam lá
- ✅ **Criativo vira tarefa** no fluxo do cliente (primeira etapa, responsável da etapa ou squad), com o prazo = dia planejado; "Mover para <próxima etapa>" / "Concluir" na tarefa; aprovação do cliente avança a etapa e ajuste volta uma; a linha da tarefa mostra "Cliente · Etapa"
- 🟡 **Planejamento** (`/social/[cliente]/[lote]/planejamento`) — calendário do lote, **tela provisória sem desenho**: primeira tela ao criar o lote, `+` no hover do dia (formato, legenda, briefing do designer), arrastar para remarcar. Troca pelo desenho do Felipe quando vier
- ✅ **Tarefas: "Minhas"** na barra e atalhos das ações rápidas (`/` + `C`/`F`)

## Fase 3 — Retenção / CRM
- ✅ **Tela de Clientes** (`/clientes`, exports "Clientes · Painel (Lista)" e "(Grade)"): a carteira da agência como entidade de verdade (`src/lib/clients/*`) — nome, segmento, serviços, responsável, dia do faturamento e a régua de saúde (Ativo · Renovação · Em risco · Pausado · Novo · VIP). Lista e Grade sobre os mesmos dados, abas por saúde, filtros (status, segmento, responsável, serviço), busca, seleção em massa e criação/edição pelo modal — o mesmo padrão de criação da tarefa
- ✅ **Ficha do cliente** (2026-09-25, export "Clientes · Detalhe", `/clientes/[id]`): clicar no cliente em `/clientes` abre a ficha, com cinco abas (`?aba=`). **Visão geral**: faturamento do mês e variação contra a fatura anterior, contrato e renovação, dia e próxima cobrança, saúde (status da carteira rebaixado por fatura atrasada, com NPS), serviços, atividades recentes, contato principal, squad e documentos. **Serviços**: tabela com escopo, responsável, progresso (meta/entregas do mês ou situação), valor e status; adicionar/editar/remover. **Financeiro**: MRR, recebido no ano, em aberto, LTV (MRR × meses de relação), histórico de faturas (marcar paga, reabrir, excluir, exportar CSV), "Gerar cobrança" (próxima competência sem fatura, no dia do faturamento, pela forma de pagamento), forma de pagamento (só bandeira, final e validade) e resumo do contrato. **Arquivos**: quatro pastas, grade/lista, busca, envio (Blob direto ou multipart), mover e excluir; a pasta Criativos junta as artes dos lotes do cliente. **Atividades**: linha do tempo montada na leitura (decisões nos lotes agrupadas, comentários nas tarefas, arquivos, faturas, eventos passados), filtros, resumo do mês (interações, resposta média a pedido de ajuste, taxa de aprovação, tarefas no prazo, NPS) e próximos eventos (marcar/desmarcar). `lib/crm/*`, régua em `view.ts`, testada
- 🟡 **Ficha do cliente — decisões minhas a confirmar com o Felipe**: (1) os formulários de serviço, evento, contrato e forma de pagamento **não têm desenho** — seguem o padrão de criação que já existe (`ClientModal`: título grande + tira de chips) e trocam quando vierem desenhados 🎨; (2) as entregas do mês de um serviço são contadas **à mão** (meta + "entregues no mês", zera sozinho na virada) — ligar às tarefas/peças aprovadas depende de um vínculo tarefa↔serviço que não existe; (3) "Contato principal" e NPS entraram como chips no modal da ficha da carteira; (4) o ícone de mensagem abre o grupo do Inbox com o nome do cliente, ou o e-mail dele; (5) "Ver projetos" abre os lotes do cliente em Social media; (6) a lista "Equipe responsável" é o squad, com a função de acesso (Admin, Editor…) — o desenho mostra cargo (Designer, Social media), que o time ainda não guarda; (7) os eventos de aprovação ganharam data ISO (`DecisionEvent.at`) e as tarefas a data de conclusão (`Task.completedAt`) — o que foi gravado antes delas fica fora da linha do tempo e do "no prazo"
- ✅ **Cartão de hover do cliente** (export "hover clientes"): passar o ponteiro sobre o nome na lista mostra status, empresa, cidade, e-mail e telefone. Cidade, e-mail e telefone entraram no modelo (e no modal de cliente) porque é o que o desenho mostra — são contato, não briefing
- ✅ **Menu de visualização de Clientes** (export "Menu de Visualização"): agrupamento (status, segmento, responsável), ordenação, Lista/Grade, mostrar arquivados, mostrar grupos vazios e os chips de colunas
- 🎨 **Briefing do cliente** — tela própria, ainda por desenhar. A arquitetura está pronta para recebê-la (o cliente já é entidade com id, e a rota cabe em `/clientes/[id]/briefing`); de propósito **não** existe nenhum campo de briefing no modelo, para não haver campo fictício a migrar depois
- ⬜ Controle **Contratado × Entregue × Saldo** (alertas de saldo negativo)
- ⬜ **Health Score** 0–100 (velocidade de aprovação, taxa de reprovação, aderência, adimplência; pesos configuráveis)
- ⬜ Timeline completa da conta
- ⬜ Relatório mensal em PDF
- 🎨 Telas de briefing e Health Score — falta desenho (a ficha e a linha do tempo do cliente já existem, ver acima)

## Transversal / Plataforma
- ✅ **Auth real** (e-mail/senha p/ agência + token p/ cliente): scrypt, cookie httpOnly assinado por HMAC, `proxy.ts` protegendo o shell, login/cadastro/recuperação, sair e troca de senha
- 🟡 Recuperação de senha registra o pedido, mas **não envia e-mail** (falta provedor)
- ✅ **Trocar a senha derruba as sessões dos outros aparelhos** (versão da senha dentro do token, sem precisar de lista de sessões)
- ✅ **Multi-tenant**: agência virou entidade com id (`lib/agency`), tarefa e lote carregam `agencyId`, e todo `repository` exige o escopo da sessão como primeiro argumento — não dá para listar nem alterar sem dizer de qual agência, e dado de outra agência responde 404. Link público do cliente segue sem sessão, restrito ao lote do token. Falta o reforço no banco (**RLS**), que entra junto com o Postgres
- 🟡 Cliente ainda é texto livre na tarefa e no lote — **e agora existe em paralelo a entidade `lib/clients`**. O fluxo de aprovação já o trata como coisa (`lib/approval/clients.ts` agrupa os lotes por slug do nome e é o que alimenta `/social`), e `/clientes` já guarda id, segmento, serviços, responsável e faturamento. **A primeira ponta já foi ligada**: `Task.clientId` e `Batch.clientId` (`resolveClientId`, em `lib/clients/repository.ts`) resolvem o texto contra a ficha cadastrada — sem acento, sem caixa, escopado por agência — toda vez que a tarefa/lote é criado ou o campo `client` muda, com teste cobrindo os dois lados (`tests/tasks-repository.test.ts`, `tests/approval-repository-client.test.ts`) e `listTasksForClient` pronto para a ficha e o Health Score. O que falta: (1) ~~tarefa/lote gravados antes desta mudança ficam com `clientId: null` até serem salvos de novo~~ — resolvido: `npm run backfill:client-ids` (`src/lib/maintenance/backfill-client-ids.ts`, testado em `tests/backfill-client-ids.test.ts`) faz esse vínculo retroativo à mão, uma vez, sem mexer em quem já está ligado; continua não sendo migração de leitura (essa é síncrona e não consultaria a lista de clientes), então quem quiser os registros antigos ligados precisa rodar o comando — não roda sozinho; (2) o campo `client` continua sendo o texto de registro — renomear a ficha do cliente **não** propaga para quem já está ligado a ela (fica certo o vínculo, desatualizado o texto mostrado); (3) nenhuma tela lê `clientId` ainda — o valor existe para a ficha do cliente e o Health Score usarem quando forem construídos, mas hoje não muda nada visível
- ✅ **Persistência em banco** (issue #11): `src/lib/store/index.ts` escolhe Postgres (Neon, `vercel:marketplace`) quando `DATABASE_URL` existe, arquivo JSON senão — os stores de cada área (tarefas, clientes, lotes, contas, mídia e, agora, Inbox) e o `transaction` (agora com `SELECT ... FOR UPDATE`, sem depender de mtime) ganharam o backend sem mexer em `repository.ts`, view ou rota. Artes: Vercel Blob com `BLOB_READ_WRITE_TOKEN`, disco/memória sem. Falta Felipe criar o banco e o Blob store na Vercel e definir as duas variáveis — sem elas a instância de produção segue em modo memória (dado some no próximo boot, mesmo risco de antes)
- 🟡 **Blob privado** (issue #39): código pronto — `putBlob` grava `access: "private"`, `GET /api/media/<id>` redireciona para URL assinada de 5 min (`presignMediaUrl`) em vez da `blobUrl` pública. Os envios diretos do navegador (arte do lote, arquivo da ficha, anexo do Inbox) também passaram a pedir privado (2026-09-25 — antes pediam público e quebrariam no store novo; `tests/media-blob-access.test.ts`). Falta só o passo que é da Vercel: criar um **novo** store Blob como Private, trocar `BLOB_READ_WRITE_TOKEN` e rodar `npm run migrate:blob-private` para copiar as artes já enviadas — ver "Store privado" no README. Até lá, todo envio é recusado com erro claro em vez de gravar público de novo
- 🟡 Filtro por agência ainda é feito em JS sobre o blob inteiro (`repository.ts`), não `WHERE agency_id`; **RLS de verdade só entra com tabela por área**, que é um passo à parte (schema relacional, não o `jsonb` de transição de agora)
- ⬜ Papéis/permissões (Coordenação, Social media, Designer, Cliente) — o papel é gravado, mas ainda não muda o que a pessoa pode fazer
- ✅ **Configurações em três abas** (2026-09-25, exports "Configurações · Pessoal / Agência / Painel da agência" do `telas.pen`; a trilha e as abas vêm das telas já construídas, porque no arquivo elas são de uma biblioteca que não vem no link):
  - **Pessoal** (todo mundo): foto (PNG/JPG/WebP, até 2 MB), nome, @, cargo (texto livre, com sugestões), papel e e-mail travados; disponibilidade; **avisos** — o que avisar (menção, atribuição, comentário, mensagem), por onde (no app / sistema, com o estado da permissão e "Enviar teste"), som (o de sempre + Gota, Sino, Estalo, Nenhum) e as conversas silenciadas com "Tirar silêncio"; **Ocupado** tira o aviso do sistema e o som, e só a menção chega no app; chamada e áudio (microfone com medidor, saída de som com "Testar", reduzir ruído, câmera com prévia, tela compartilhada, atalho de mudo) — a chamada começa com essas escolhas e grava de volta o que for trocado nela; senha com força e confirmação; sessão atual (navegador, cidade pela Vercel) e **Sair de todos os aparelhos**. Um "Salvar alterações" para a aba toda, com a contagem do que mudou
  - **Agência** (Admin e Gerente): logo (aparece no link de aprovação do cliente), nome (renomeia para todas as contas), domínio do convite automático e a função de quem entra por ele, atalhos para Membros e Fluxos com contagens, **padrões para cliente novo** (dia de cobrança, reajuste, fidelidade, forma de pagamento, ciclo, fluxo — entram no cadastro e na ficha do cliente novo), status das integrações (nunca a chave) e **Excluir agência** (só Admin, digitando o nome; apaga a agência de todas as áreas — `lib/agency/delete.ts`, testado contra a agência vizinha)
  - **Painel da agência** (Admin, Gerente e Financeiro): tira de números, operação (tarefas por status, carga por pessoa), aprovação (pendentes, decisões do mês, tempo até decidir, taxa de ajuste, lotes esperando), financeiro (**só Admin e Financeiro** — para o Gerente o bloco vem trancado e sem valor no HTML), carteira (por status, sem fluxo, em risco com o motivo), entregas do mês por unidade, equipe (pedidos de entrada com Aprovar/Recusar, último acesso) e agenda (os eventos das fichas). Filtros de mês, cliente e pessoa na URL; "Exportar" baixa um CSV. Tudo calculado na hora em `lib/dashboard/metrics.ts`, testado
- 🟡 **Configurações — decisões minhas a confirmar com o Felipe**: (1) **meta de "Concluídas no prazo" fixa em 85%** (`ON_TIME_GOAL`) — não há campo para mudar; (2) o logo **não** entra na barra lateral (o topo dela é o logo do produto, e trocar isso é desenho novo) — só no link de aprovação; (3) **sem SVG** no logo e na foto: o arquivo é servido pelo próprio app e SVG pode carregar script; (4) o KPI "Tarefas abertas" diz quantas foram **criadas** no mês, e não "N a mais que em agosto" — sem histórico, não dá para saber quantas estavam abertas no fim do mês passado; (5) o "Seu @" mostra o status como selo, como no desenho; (6) a lista de sessões mostra só este aparelho — o app não guarda a lista de sessões abertas, e "Sair de todos" derruba todas pela versão da conta
- ❌ **Descartados pelo Felipe** (2026-09-25): o bloco "NPS médio · N respostas" da Carteira (a ficha guarda uma nota por cliente, não as respostas) e "Exportar dados da agência" (o app não envia e-mail)
- ✅ **Inbox — a conversa do time** (issue #30, export "Inbox"): grupos e diretas estilo Discord numa tela só, histórico salvo e pesquisável (na lista e dentro da conversa), não lidas, silenciar, presença (disponível · ocupado · ausente · offline — **forma** do símbolo carregando o estado e a cor da escala de saúde do cliente por cima, trocada no menu da sua conta) e a chamada no popup dos dois avatares, que deixa o registro no histórico. Só o time da agência entra; cliente segue decidindo pelo link público. `src/lib/inbox/*` + `src/components/inbox/*` + `/api/inbox/*`
- ✅ **Camada de tempo real** (issue #43): **Ably** para os eventos (mensagem nova, presença de verdade — quem está com o app aberto —, aviso de chamada em andamento) e **LiveKit** para a mídia da chamada (áudio + tela compartilhada, com TURN). Os dois atrás de `src/lib/realtime/*` e opcionais por variável de ambiente: sem elas o Inbox volta à releitura de 12s e a chamada abre sem transmitir. O token do Ably é emitido pelo servidor, canal a canal, sem curinga (`tests/realtime-channels.test.ts`); o crachá do LiveKit vale só para a sala daquela conversa
- ✅ **Tela da chamada** (export "Chamada · Call View", 2026-09-25): no desktop a chamada aberta toma o lugar da conversa no Inbox (lista à esquerda continua), no celular cobre a tela. Cabeçalho com "Sala de reunião"/"Chamada com X", cronômetro, grade × palco, voltar ao chat, tela cheia e "⋯"; faixa de quadros (você, quem está na sala, quem está sendo chamado na direta) com quem fala aceso; **palco** de quem apresenta com o selo "X está apresentando" e a qualidade que está chegando de verdade (ex. "1080p · 30fps"); painel de participantes; barra Microfone (menu de aparelho, saída de som, reduzir ruído) · Câmera · Apresentar · Reagir (emoji que aparece no quadro de quem mandou, pelo canal de dados da sala) · Participantes · Configurações · Sair. `src/components/inbox/CallOverlay.tsx`
- ✅ **Os três modais de controle** (mesmo export): **Compartilhar tela** (Telas · Janelas · Aba, resolução, quadros, áudio do sistema), **Câmera** (prévia ao vivo, aparelho, desfoque, espelhar, iluminação, resolução) e **Qualidade da transmissão** (resolução, quadros, bitrate, aceleração por hardware). As escolhas ficam guardadas no navegador. `src/components/inbox/CallModals.tsx` + `src/lib/inbox/{screenQuality,callPrefs,devices}.ts`
- 🟡 **Decisões da tela da chamada para o Felipe confirmar**:
  - **Compartilhar tela não lista as telas e janelas** como o export (Tela inteira, Chrome, Figma, VS Code): o navegador não deixa um site ver o que está aberto. As abas escolhem o que o seletor do navegador (ou o do app de desktop) oferece primeiro, e a escolha de verdade acontece nele
  - **Desfoque de fundo e ajuste de iluminação** só ligam quando a câmera/navegador oferece (Chrome com câmera compatível); fora disso aparecem desligados, dizendo por quê — não há processamento de imagem próprio no app
  - **Aceleração por hardware** fica desligada no modal: o navegador decide sozinho quando usar a GPU e não deixa o site escolher
  - **O botão de chat do cabeçalho** recolhe a chamada no cartão do canto e volta para a conversa; abrir outra conversa com a chamada aberta faz o mesmo
  - **Esc não encerra mais a chamada** (fecha menu/modal); sair é só pelo botão. F alterna a tela cheia
  - **"Configurações" da barra abre o modal Qualidade da transmissão**; microfone e saída de som ficam na setinha do Microfone, como no Discord
  - Sem provedor de mídia (LiveKit), os controles aparecem desligados com o motivo; a sala, o tempo e o registro no histórico continuam
- ✅ **Chamada que dura e chamada abandonada**: quem está na chamada manda um "ainda estou aqui" a cada 30s, e quem fica 2 min calado sai sozinho — a chamada longa não expira mais, e a abandonada (navegador travado, bateria) fecha com a linha no histórico, contando até o último sinal. `src/lib/inbox/call.ts`
- ✅ **Mesma pessoa em duas abas**: entrar pela segunda aba traz a chamada para ela; a primeira avisa "Chamada em outra aba" e sai sem tirar você da chamada
- ✅ **Criar grupo / adicionar alguém**: o botão de pessoa do cabeçalho (no celular, pelo menu "…"). Numa direta cria um grupo com as três pessoas (a direta fica como estava); num grupo, adiciona. Grupo sem nome se chama por quem está nele. Quem foi incluído fica sabendo na hora pelo canal pessoal do tempo real
- 🎨 **Grupo: nome, foto, tirar alguém, sair do grupo** — falta desenho; hoje o grupo criado aqui fica sem nome (título = quem está nele)
- 🟡 **Falta provar a transmissão numa rede que deixe**: a sessão em que isso foi construído tem política de saída que bloqueia `rest.ably.io` e o host do LiveKit (e não passa WebSocket), então áudio e tela foram validados até onde dava — crachá correto, sala certa, entrar/sair, registro no histórico e a degradação sem provedor. O teste de ponta a ponta (duas máquinas, redes diferentes) é o que falta, e ele roda no `next dev` do Felipe ou num preview da Vercel com as quatro variáveis definidas
- ⬜ Notificação de chamada recebida fora do app (o Felipe providencia)
- ✅ **Aviso no PC que não chegava** (2026-09-23): o "você já está vendo" usava só a aba visível — e no app de desktop a página sempre se diz visível (`backgroundThrottling: false`), então com o Inbox aberto e a janela minimizada/na bandeja nenhum aviso saía, e a conversa ainda era dada por lida sozinha. Agora exige **foco da janela** e vale só para a conversa aberta; sem permissão de notificação no sistema, o aviso vira toast com "Abrir"
- ✅ **Mensagens: responder, editar e apagar** — no hover da mensagem, "responder" e o "⋯" (responder, copiar, editar, apagar). Editar e apagar só a sua, **até 10 minutos** depois de enviada (`MESSAGE_EDIT_WINDOW_MS`); apagada fica a marca "Mensagem apagada" (e o aviso dela para de mostrar o texto); editada ganha "(editada)"
- ✅ **Anexos, voz e GIF na conversa** — clipe, arrastar ou colar; até 20 MB e 10 por mensagem, pelo mesmo armazenamento das artes (direto para o Blob em produção, pasta `anexos/`); tipos fechados (imagem, vídeo, áudio, PDF, Office, texto, ZIP — nada executável). Microfone grava a mensagem de voz (até 5 min). GIF pelo **Tenor** (`TENOR_API_KEY`) ou **Giphy** (`GIPHY_API_KEY`) — sem chave, o botão explica o que configurar. Emoji segue 🎨
- ✅ **Inbox no celular** (2026-09-24): a conversa abre em tela cheia por cima da lista (sem a barra do app), e o "voltar" do aparelho volta para a lista. Cabeçalho só com voltar, ligar e "⋯" (o resto — tela compartilhada, busca, adicionar alguém, silenciar, não lida — numa folha que sobe de baixo, `ui/ActionSheet`). Mensagem: **segurar** abre responder/copiar/editar/apagar na folha, **arrastar para a direita** responde; a barra de hover não aparece no toque. Texto de 15px e campo de 16px (sem o zoom do iPhone), botões de 36–40px, área segura do iPhone respeitada. Provisório no vocabulário v3 — sem export mobile do Inbox ainda
- 🟡 **Chave de GIF**: definir `GIPHY_API_KEY` na Vercel para a biblioteca aparecer (a chave do Felipe foi testada em 2026-09-25 e responde). O Tenor anunciou o fim da API — a v1 já responde "discontinued" —, então `TENOR_API_KEY` não vale mais a pena
- ✅ **Player da mensagem de voz** (export `Inbox · Áudio-export.html`, 2026-09-25): botão claro de tocar/pausar, forma de onda de verdade (56 barras — o que já tocou em branco), "0:16 / 0:42" e o selo de velocidade 1× → 1.5× → 2×. A onda é a barra de progresso: clicar/arrastar leva até ali; pelo teclado é slider (setas andam 5 s). Duração e barras são medidas no navegador de quem manda e vêm no anexo (`src/lib/inbox/voice.ts`, `components/inbox/audioAnalysis.ts`); mensagem antiga é medida por quem ouve. No celular a onda perde barras para caber. Uma mensagem de voz toca de cada vez
- 🟡 **Transcrição da mensagem de voz** ("Ver transcrição" do export): falta decidir o provedor de fala-para-texto — o áudio sai para um serviço de fora e tem custo por minuto. O botão só entra quando houver transcrição para mostrar
- ✅ **Quem está na chamada, claro** — faixa "AO VIVO" na conversa com os avatares de quem está dentro e quantos estão fora; selo com telefone e número na lista de conversas; no popup, cada pessoa "na chamada" (acesa) ou "chamando…/fora da chamada" (apagada e tracejada)
- ✅ **App de desktop (Electron)** — pasta `desktop/`: janela que abre o black berry publicado (deploy novo chega sem reinstalar), fica na bandeja ao fechar (Windows/macOS) para o Inbox e a chamada seguirem vivos, atalho global de mudo Ctrl/Cmd+Shift+M só durante a chamada, microfone/câmera/tela liberados só para a origem do app e link externo saindo para o navegador. A página fala com ele por `window.blackberryDesktop` (`src/lib/desktop.ts`). Ver `desktop/README.md`
- ✅ **Logo oficial** — o arquivo do Felipe em `public/brand/blackberry-logo.svg` (e só o símbolo em `blackberry-mark.svg`); no site pelo componente `Logo`/`LogoMark` (`src/components/brand/Logo.tsx`: lateral, barra do celular, login, link público de aprovação), favicon em `src/app/icon.svg`, e no desktop como ícone do app e da bandeja. Cores `--color-brand` / `--color-brand-ink`
- ✅ **Desktop: escolher o que compartilhar** — seletor com abas Janelas/Telas, miniaturas que se atualizam e "Compartilhar o som" (Windows), no jeito do Discord, pedido pelo Felipe (`desktop/src/screen-picker.js` + `src/picker/`). No macOS 15+ segue o seletor do sistema. Sem export do pen.dev ainda — se o Felipe desenhar, é trocar o `picker.html`/`.css`
- ✅ **Desktop: barra de título preta** — a barra do sistema some e a janela desenha a sua em `#000000`, com os botões do sistema por cima; o site fica numa `WebContentsView` logo abaixo
- ✅ **Desktop: endereço de produção** — `blackberry.url` = `https://ragick-xi.vercel.app`
- ⬜ **Desktop: assinatura** — Felipe decide se compra os certificados (Apple Developer, assinatura de código Windows); sem eles o instalador abre com aviso de segurança
- ✅ **Notificações** (`/notificacoes`, pedido do Felipe em 2026-09-23): menção (briefing, comentário, mensagem), tarefa atribuída (na mão ou pelo fluxo), comentário na sua tarefa e mensagem nova (várias da mesma conversa viram um aviso só, com contador). Abas Todas/Não lidas/Menções/Atribuições, marcar lida/não lida, tirar da lista, "marcar todas". Abrir a tarefa ou a conversa dá por lidos os avisos dela; número na lateral; aviso do sistema pelo canal pessoal do tempo real (ou releitura). `src/lib/notifications/*` — régua em `rules.ts`/`view.ts`, testada
- 🎨 **Desenho da tela de Notificações** — a tela foi montada com o vocabulário v3 que já existe (painel, abas, linhas, blocos por dia do Inbox); é placeholder funcional, fácil de trocar quando o Felipe desenhar
- ⬜ Relatórios (placeholder; saiu da lateral)
- ✅ **Usuários** (`/equipe`, export "Usuários · Painel (Lista)"): função, status, último acesso; convite com link que entra na agência; **convite automático pelo domínio** (com aprovação, porque não há confirmação de e-mail); arquivar tira o acesso na hora
- ✅ **@ por pessoa** (menção e atribuição no briefing, comentário, Inbox e responsável) · **chamada minimizável** no canto, como no Discord · não lidas do Inbox na lateral
- ✅ **Design system v3** em Social media, Lote, Editor, Planejamento e Configurações; link público do cliente no celular fiel ao export mobile
- ✅ **Testes automatizados** (`tests/`, runner do próprio Node, 469 casos das funções puras, incluindo o isolamento entre agências e a régua de clientes) **+ CI** (`.github/workflows/ci.yml`: tipos, testes e build a cada push e pull request)
- ✅ **`npm run lint` volta a funcionar**: o Next 16 removeu o `next lint`; entrou ESLint direto (`eslint.config.mjs`, flat config) com `eslint-config-next/core-web-vitals` + `/typescript`. Duas regras novas do plugin de React Compiler (`react-hooks/set-state-in-effect`, `react-hooks/refs`) ficaram desligadas — pegam padrões usados de propósito em várias telas (sincronizar estado com uma prop que muda, ler `ref.current` para medir menu/toolbar); satisfazê-las seria reescrever esses componentes, não corrigir lint. Ainda fora do CI, que continua só com tipos/testes/build.

## Fora do MVP (não construir sem pedido)
Agendamento/publicação automática · métricas de redes · financeiro · NF/boleto · timesheet · CRM de prospecção.

---

## Rotina do Claude (como trabalhar a cada sessão)

Quem decide o quê e como o trabalho passa entre Felipe e Claude está em `FLUXO.md` — leia antes de começar. Resumo do que vale para toda sessão:

1. **Contexto primeiro.** Ler `FLUXO.md`, este `ROADMAP.md` e as memórias do projeto (começar por `ragick-black-berry-redesign`).
2. **Design é lei, e é sempre do Felipe.** A fonte de verdade são os `*-export.html` (pen.dev) na raiz. Construir fiel ao design. Tela sem desenho fica como **placeholder fácil de trocar** — nunca inventar tela final por conta própria; marcar 🎨 aqui.
3. **Arquitetura estável.** Novas views são *leitoras* de `repository`/`constants`. Nunca duplicar dados por tela. Pipeline de status só em `constants.ts`.
4. **Interações + feedback.** Toda ação relevante dá feedback (toast, estado otimista, animação sutil). Respeitar `prefers-reduced-motion`.
5. **Validar sempre.** `npm run typecheck` + `npm test` + `npm run build` verdes; app rodando de verdade (não só compilando) nas rotas tocadas, desktop e celular.
6. **Deploy + versionamento.** Commits pequenos e descritivos; branch própria + PR (ver `FLUXO.md` para quando o merge é do Felipe).
7. **Fechar o loop.** Atualizar este roadmap; relatar o que foi feito, o que ficou pendente e o que precisa de decisão/desenho.

### Próximos passos sugeridos (ordem)
1. **Felipe cria o Neon Postgres e o Vercel Blob store** (Vercel → Storage) e define `DATABASE_URL`/`BLOB_READ_WRITE_TOKEN` — o código dos dois já está pronto (issue #11); sem as variáveis a produção continua em modo memória.
2. **Felipe recria o Blob store como Private** (issue #39): o store atual é público e a Vercel não deixa converter — precisa de um store novo, trocar `BLOB_READ_WRITE_TOKEN` e rodar `npm run migrate:blob-private` (copia as artes; passo a passo no README). O código (`putBlob` privado, URL assinada em `GET /api/media/<id>`) já está pronto e recusa upload com erro claro enquanto o store continuar público, em vez de voltar a gravar exposto.
3. **Schema relacional + RLS**: hoje cada área é um `jsonb` inteiro por linha (drop-in seguro, zero mudança de view/rota); virar tabela de verdade por área (`WHERE agency_id`, política de RLS) é o passo que fecha a dívida de multi-tenant no banco.
4. ~~Ligar tarefa e lote ao `Client.id`~~ — resolvido: `Task.clientId`/`Batch.clientId` (ver "Dívidas conhecidas"), com backfill (`npm run backfill:client-ids`) para o que foi gravado antes. Falta rodar o backfill em produção (ele não roda sozinho) e alguma tela consumir o vínculo — é o que destrava Health Score, timeline da conta e o filtro por cliente de verdade.
5. **Definir `ABLY_API_KEY` e as três do LiveKit na Vercel** (produção e preview) e conferir a chamada entre duas máquinas em redes diferentes — o código está pronto, falta a prova numa rede que deixe passar.
6. **Calendário editorial** lendo a data de publicação que o editor já grava, e a carga do time a partir do responsável.
7. **Envio automático do link** (WhatsApp/e-mail pelo servidor) + lembretes — depende da decisão de provedor (issue #13) e da ficha do cliente (#3, 🎨) para ter contato de verdade.
8. Continuar o CRM/Health Score (Fase 3) sobre a entidade de cliente que acabou de nascer.

### Dívidas conhecidas
- `AUTH_SECRET` não está definido na Vercel. O código já não aceita mais rodar assim: em produção sem o segredo o servidor recusa subir (`src/instrumentation.ts`) e assinar/conferir cookie lança (`src/lib/auth/token.ts`) — não existe mais o silêncio de cair no segredo de desenvolvimento. Falta o Felipe definir a variável no painel (`openssl rand -base64 32` → Settings → Environment Variables); até lá, a instância em produção não sobe.
- O `json-file.ts` relê pelo mtime, mas dois processos ainda podem se sobrepor num leitura-altera-grava simultâneo: é o preço de arquivo como banco. **Some com o Postgres** (`postgres.ts`, lock de linha) assim que `DATABASE_URL` estiver definida — até lá, produção sem a variável segue no arquivo/memória de sempre.
- As artes são servidas como foram enviadas, sem derivadas leves.
- **Confirmação de e-mail não existe.** É por isso que o convite automático pelo domínio exige aprovação; quando houver envio de e-mail (issue #13), a entrada pelo domínio pode passar a ser direta.
- **Freio do login é por instância** (em memória): sobe o custo de adivinhar senha, não impede. O definitivo é uma regra de rate limit no firewall da Vercel.
- **Contraste dos selos de status da tarefa.** O export v3 desenha "Concluído" (#565656), "Pausado" (#5a5a5a) e "Cancelado" (#404040) sobre o fundo #1f1f1f do selo — entre 1,3:1 e 1,7:1, bem abaixo dos 4,5:1. Está implementado exatamente como desenhado; se a leitura incomodar, é uma decisão de design do Felipe (clarear o texto ou escurecer o fundo), não minha.
- **O menu de visualização de Clientes tem três leituras minhas, e o Felipe decide se ficam.** (1) A seção "Filtros" do desenho tem o rótulo "Agrupamento" com uma pílula escrita "Status", e "Agrupamento" já existe na seção Organização logo abaixo — implementei como atalho de filtro por status (rótulo "Status", pílula "Todos"), porque é a leitura que não deixa duas linhas iguais fazendo coisas diferentes. (2) "Mostrar arquivados" esconde os **pausados**: cliente não tem estado de arquivo, e Pausado é o degrau que faz esse papel — a chave nasce ligada, para a lista não começar escondendo cliente. (3) Dos chips de coluna apagados do desenho (Vencimento, Renovação, Risco, Tags, Criado em, Atualizado em) só "Criado em" existe: os outros não têm campo no modelo, e chip que não liga nada é pior que chip que não existe
- O store `ragick-artes` no Vercel Blob é público (criado pela issue #11) e a Vercel não deixa converter o modo de acesso depois — issue #39 fecha isso com um store novo, privado, que Felipe ainda precisa criar e migrar. Até lá o código recusa novos uploads em vez de aceitar público de novo (ver "Store privado" no README).
- ~~Não existe convite de equipe~~ — resolvido: convite por link (Usuários) e convite automático pelo domínio da agência.
