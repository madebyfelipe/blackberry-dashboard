# dependencies
/node_modules
/desktop/node_modules

# instaladores gerados pelo electron-builder
/desktop/dist
/.pnp
.pnp.*

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env*
!.env.example

# typescript
*.tsbuildinfo
next-env.d.ts

# local data (JSON store — see memory ragick-persistence-deploy)
/data

.vercel
.env*
# O modelo de variáveis é o único .env que entra — sem valor nenhum dentro.
!.env.example

# worktrees dos agentes (o harness as cria dentro do repo)
/.claude/*
!/.claude/settings.json
